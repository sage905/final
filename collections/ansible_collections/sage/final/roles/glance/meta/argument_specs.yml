---
argument_specs:
  main:
    short_description: Deploy Glance behind the panel role's shared Caddy.
    options:
      glance_data_dir:
        type: path
        default: /srv/glance
      glance_fqdn:
        type: str
        required: true
      glance_image:
        type: str
        default: docker.io/glanceapp/glance:latest
      glance_container_port:
        type: int
        default: 8080
      glance_seed_config:
        type: bool
        default: true
      glance_caddy_conf_d_dir:
        type: path
        default: /srv/caddy/conf.d
      glance_caddy_container:
        type: str
        default: caddy
      glance_shared_network:
        type: str
        default: web
      glance_install_podman:
        type: bool
        default: true
      glance_user:
        type: str
        default: containers
