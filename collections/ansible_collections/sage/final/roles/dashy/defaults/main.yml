---
dashy_data_dir: /srv/dashy
dashy_image: docker.io/lissy93/dashy:latest
dashy_container_port: 8080

# Initial conf.yml is templated on first run and then left alone; edit
# {{ dashy_data_dir }}/user-data/conf.yml directly after deploy.
dashy_seed_config: true

# Shared infra provided by sage.final.caddy.
dashy_shared_network: web

# Docker is installed once per host by the dedicated play in site.yml. Leave
# this false; set true only to install Docker from this role on a standalone run.
dashy_install_docker: false

# Rootful: stack runs via system Docker. dashy_user owns the files on
# disk. The Dashy v4 image runs as the non-root "node" user (UID 1000),
# so it cannot rely on owner access; instead the container joins the
# dashy group (via group_add in the compose template) and reaches the
# group-owned files through group-write modes set below.
dashy_user: root

# Group that owns Dashy's files. Admins in dashy_group_members can read
# and write /srv/dashy via SFTP (setgid + group-write modes set below).
dashy_group: dashy
dashy_group_members: []
