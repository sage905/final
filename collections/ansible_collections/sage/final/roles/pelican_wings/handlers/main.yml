---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart wings
  ansible.builtin.systemd:
    name: wings
    state: restarted
  when: pelican_wings_config_stat.stat.exists | default(false)
